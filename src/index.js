import validator from './validator.js';

console.log(validator);

//traer elemento de formulario y declarar la funcion onclick 
 
function sendValue(){
    const cardNumber = document.getElementsByClassName("validationbutton");
    cardNumber = prompt("funciona");
}

 